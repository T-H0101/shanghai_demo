# ============================================================
# Docker PostgreSQL 17 - 统一平台中心库
# Sprint 2B.1 - 本地开发环境
# ============================================================
#
# 用途：
#   - 本地开发验证 PostgreSQL 连接
#   - 中心库 Schema 初始化
#   - 不连接真实源库，不同步真实数据
#
# 使用方法：
#   pnpm db:up
#   pnpm db:logs
#   pnpm db:down:volumes  # 停止并删除数据卷
#
# 如果直接使用 docker compose, 必须显式加载 .env.local:
#   docker compose --env-file .env.local up -d postgres
#   docker compose --env-file .env.local logs postgres
#   docker compose --env-file .env.local down -v
#
# 密码：
#   - 默认 dev (Sprint 2B.1)
#   - pnpm db:* 已统一读取 .env.local
#   - 生产/真实环境必须 POSTGRES_PASSWORD env 注入 (强密码)
#   - 轮换流程见 docs/secrets/SECRETS.md (gitignored)
# ============================================================
name: ${COMPOSE_PROJECT_NAME:-unified-disc-platform}

services:
  postgres:
    image: postgres:17
    container_name: ${POSTGRES_CONTAINER_NAME:-unified_disc_postgres}
    restart: unless-stopped
    environment:
      POSTGRES_USER: unified
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD:-unified123}
      POSTGRES_DB: unified_disc_platform
      POSTGRES_INITDB_ARGS: "-E UTF8"
    ports:
      - "${POSTGRES_PORT:-5432}:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U unified -d unified_disc_platform"]
      interval: 10s
      timeout: 5s
      retries: 5
      start_period: 10s
    networks:
      - unified_disc_network

  opensearch:
    image: opensearchproject/opensearch:2
    container_name: unified_disc_opensearch
    restart: unless-stopped
    environment:
      - discovery.type=single-node
      - plugins.security.disabled=true
      - OPENSEARCH_INITIAL_ADMIN_PASSWORD=local-dev-only
      - DISABLE_INSTALL_DEMO_CONFIG=true
    ulimits:
      memlock:
        soft: -1
        hard: -1
    ports:
      - "9200:9200"
    networks:
      - unified_disc_network

  clickhouse:
    image: clickhouse/clickhouse-server:24
    container_name: unified_disc_clickhouse
    restart: unless-stopped
    environment:
      - CLICKHOUSE_DB=unified_logs
      - CLICKHOUSE_USER=default
      - CLICKHOUSE_PASSWORD=local-dev-only
      - CLICKHOUSE_DEFAULT_ACCESS_MANAGEMENT=1
    ulimits:
      nofile:
        soft: 262144
        hard: 262144
    ports:
      - "8123:8123"
      - "9000:9000"
    networks:
      - unified_disc_network

volumes:
  postgres_data:
    driver: local

networks:
  unified_disc_network:
    driver: bridge
