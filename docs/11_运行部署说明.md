# 统一光盘库管理平台 - 运行部署说明

> ⚠️ 本文档面向开发和运维人员，说明如何本地运行和部署本项目。

---

## 1. 环境要求

### 1.1 运行时环境

| 组件 | 版本要求 | 说明 |
|------|----------|------|
| Node.js | ≥ 18.17.0 | 推荐使用 LTS 版本 |
| npm | ≥ 9.0.0 | 随 Node.js 一起安装 |
| 操作系统 | macOS / Linux / Windows | 支持主流操作系统 |

### 1.2 开发工具（可选）

| 工具 | 版本 | 说明 |
|------|------|------|
| VS Code | 最新稳定版 | 推荐配置 ESLint 和 TypeScript 插件 |
| Chrome / Edge | 最新稳定版 | 用于浏览器调试 |

### 1.3 检查环境

```bash
# 检查 Node.js 版本
node --version

# 检查 npm 版本
npm --version

# 预期输出示例：
# v20.12.0
# 10.5.0
```

---

## 2. 安装步骤

### 2.1 克隆项目

```bash
# 进入项目目录
cd /Users/tian/Desktop/上海

# 查看项目结构
ls -la
```

### 2.2 安装依赖

```bash
# 安装所有依赖
npm install

# 预期输出：
# added 312 packages in 30s
# added 87 packages, dag size is 142MB
```

### 2.3 安装问题排查

| 问题 | 解决方案 |
|------|----------|
| npm install 报错 | 删除 `node_modules` 和 `package-lock.json`，重新执行 |
| 依赖版本不匹配 | 使用 `npm install --legacy-peer-deps` |
| 网络问题 | 配置国内镜像：`npm config set registry https://registry.npmmirror.com` |

---

## 3. 开发命令

### 3.1 常用命令

| 命令 | 说明 | 访问地址 |
|------|------|----------|
| `npm run dev` | 启动开发服务器 | http://localhost:3000 |
| `npm run build` | 生产构建 | 生成 `.next/` 目录 |
| `npm run start` | 启动生产服务器（需先 build） | http://localhost:3000 |
| `npm run lint` | 代码检查 | - |
| `npm run type-check` | TypeScript 类型检查 | - |

### 3.2 启动开发服务器

```bash
npm run dev

# 预期输出：
# ▲ Next.js 16.2.6
# - Local: http://localhost:3000
# - Ready in 1.5s
```

### 3.3 生产构建

```bash
npm run build

# 预期输出：
# ✓ Compiled successfully
# ✓ Collecting page data
# ✓ Generating static pages
# ✓ Build completed
```

---

## 4. 路由访问

### 4.1 页面路由清单

| 页面 | 路由 | 说明 |
|------|------|------|
| 首页仪表盘 | `/` | 统计卡片、图表、任务表格 |
| 站点管理 | `/sites` | 站点列表、搜索、筛选、新建 |
| 盘架管理 | `/racks` | 盘架列表、槽位可视化 |
| 任务管理 | `/tasks` | 任务列表、CRUD 操作 |
| 用户与权限 | `/users` | 用户列表、权限树 |
| 审计日志 | `/logs` | 日志 Tab、筛选、导出 |
| 统一检索 | `/search` | 检索表单、结果表格 |
| 系统设置 | `/settings` | 配置表单、服务监控 |

### 4.2 访问验证

开发服务器启动后，在浏览器中访问：

```
http://localhost:3000/          → 首页仪表盘
http://localhost:3000/sites     → 站点管理
http://localhost:3000/tasks      → 任务管理
```

---

## 5. 目录结构

```
/Users/tian/Desktop/上海/
├── app/                    # Next.js App Router 页面
│   ├── layout.tsx          # 根布局
│   ├── page.tsx            # 首页
│   ├── sites/page.tsx      # 站点管理
│   ├── tasks/page.tsx      # 任务管理
│   ├── users/page.tsx      # 用户与权限
│   ├── logs/page.tsx       # 审计日志
│   ├── search/page.tsx     # 统一检索
│   ├── racks/page.tsx      # 盘架管理
│   └── settings/page.tsx   # 系统设置
├── components/             # React 组件
│   ├── ui/                # 基础 UI 组件（57 个）
│   ├── dashboard/          # 仪表盘组件
│   ├── platform/          # 平台通用组件
│   └── layout/            # 布局组件
├── lib/                    # 工具库
│   ├── mock/              # Mock 数据（7 个文件）
│   ├── types/             # TypeScript 类型定义
│   └── utils.ts           # 工具函数
├── hooks/                  # React Hooks
├── store/                  # 状态管理（通知中心）
├── docs/                   # 项目文档
├── package.json
├── tsconfig.json
├── tailwind.config.ts
└── next.config.mjs
```

---

## 6. 配置文件

### 6.1 关键配置文件

| 文件 | 用途 |
|------|------|
| `package.json` | 项目依赖、脚本命令 |
| `tsconfig.json` | TypeScript 配置 |
| `next.config.mjs` | Next.js 配置 |
| `tailwind.config.ts` | Tailwind CSS 配置 |
| `postcss.config.mjs` | PostCSS 配置（Tailwind） |

### 6.2 环境变量（可选）

如需配置环境变量，创建 `.env.local` 文件：

```bash
# .env.local 示例
NEXT_PUBLIC_API_BASE_URL=http://localhost:8080
```

---

## 7. 常见问题

### 7.1 启动问题

**Q：执行 `npm run dev` 报错 "Cannot find module"**

A：执行 `npm install` 重新安装依赖。

**Q：端口 3000 被占用**

A：使用其他端口：`npm run dev -- -p 3001`

**Q：页面样式异常**

A：清除缓存，重新构建：
```bash
rm -rf .next node_modules/.cache
npm run build
npm run dev
```

### 7.2 构建问题

**Q：执行 `npm run build` 失败**

A：检查 TypeScript 错误：`npm run type-check`

**Q：build 成功但页面空白**

A：检查浏览器控制台，可能是资源路径问题。

### 7.3 数据问题

**Q：页面数据不显示**

A：确认 Mock 数据文件路径正确，检查浏览器控制台 Network 面板。

**Q：新建的数据消失了**

A：正常现象，Mock 数据仅存在于内存，刷新页面会重置。

---

## 8. 部署建议

### 8.1 静态部署（当前 Demo）

当前 Demo 为纯前端项目，可通过以下方式部署：

**本地预览构建结果**：
```bash
npm run build
npm run start
```

**Docker 部署**：
```dockerfile
FROM node:20-alpine AS builder
WORKDIR /app
COPY package*.json ./
RUN npm ci
COPY . .
RUN npm run build

FROM node:20-alpine AS runner
WORKDIR /app
COPY --from=builder /app/.next ./.next
COPY --from=builder /app/public ./public
COPY --from=builder /app/node_modules ./node_modules
EXPOSE 3000
CMD ["npm", "start"]
```

### 8.2 未来后端集成

正式生产环境需要接入真实后端，建议架构：

```
┌─────────────┐     ┌─────────────┐     ┌─────────────┐
│   前端       │ ←→  │   API GW    │ ←→  │   后端服务   │
│  (Next.js)   │     │  (Nginx)    │     │  (Java/.NET)│
└─────────────┘     └─────────────┘     └─────────────┘
                                               ↓
                                        ┌─────────────┐
                                        │  数据库      │
                                        │ (PostgreSQL)│
                                        └─────────────┘
```

**接口替换**：
- 将 `lib/mock/` 中的数据获取替换为 `fetch('/api/xxx')`
- 使用 React Query 管理数据请求和缓存
- 添加 WebSocket 实时推送

### 8.3 生产环境检查清单

| 检查项 | 说明 |
|--------|------|
| HTTPS | 生产环境必须使用 HTTPS |
| 环境变量 | 配置 API 地址、密钥等 |
| 日志 | 接入日志系统 |
| 监控 | 接入前端监控（如 Sentry） |
| CDN | 静态资源部署到 CDN |
| 域名 | 配置正式域名 |

---

## 9. 技术栈版本

| 技术 | 版本 | 用途 |
|------|------|------|
| Next.js | 16.2.6 | React 框架 |
| React | 19.x | UI 库 |
| TypeScript | 5.x | 类型检查 |
| Tailwind CSS | v4 | 样式框架 |
| Radix UI | 最新 | 无样式组件库 |
| Recharts | 最新 | 图表库 |
| Lucide React | 最新 | 图标库 |
| React Hook Form | 最新 | 表单处理 |
| Zod | 最新 | 数据校验 |

---

*文档生成时间：2026-05-18*
*基于 Demo 阶段代码状态*