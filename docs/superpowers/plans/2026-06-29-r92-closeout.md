# R.92 Closeout — Final Delivery Verification Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development to implement this plan task-by-task.

**Goal:** Make PR #7 reviewable, mergeable, and presentable to leadership — local dev loop fully green, no developer terminology in user-visible text, deployment docs honest and complete.

**Architecture:** Continue on current branch (`codex/r84-development-architecture-cleanup-plans`), add R.92 commits. No merge until all gates pass.

**Tech Stack:** Next.js 16.2.6, React 19, TypeScript, PostgreSQL 17, tsx for standalone scripts, dotenv for env loading.

## Global Constraints

- `.env.local` is the source of truth for all DB connection config
- DATABASE_URL, POSTGRES_PASSWORD, DB_PASSWORD must be consistent (env:check enforces)
- postgres.ts must fail-closed if DATABASE_URL is missing (no hardcoded fallback)
- All user-visible text must use product language (站点代理, not Agent; not mock, not 真实, not sync_package, not blocked_by_*)
- /volumes links must go to /racks?view=volumes (no standalone volumes page links)
- /check links must go to /racks?view=inspection (no 17-tab page)
- Old self-checks that validate 17-tab /check or standalone /volumes must be deleted
- Requirements honesty: don't claim complete for blocked items
- Commit messages must include R.92 reference

---

### Task 1: env:init + env:check scripts + postgres.ts fail-closed

**Files:**
- Create: `scripts/env/init.ts`
- Create: `scripts/env/check.ts`
- Modify: `lib/db/postgres.ts:21` (remove hardcoded fallback)
- Modify: `package.json` (add env:init, env:check scripts)

**Interfaces:**
- Produces: `pnpm env:init` command, `pnpm env:check` command, `pnpm env:check:production` command
- postgres.ts: throws if DATABASE_URL is missing instead of falling back

**Steps:**

1. Create `scripts/env/init.ts`:
   - If `.env.local` exists and no `--force` flag, print "已存在, 使用 --force 重建" and exit 0
   - Copy `.env.example` to `.env.local`
   - Generate random 32-byte secrets for AUTH_SESSION_SECRET, SYNC_PACKAGE_SECRET, ADMIN_TOKEN, SITE_AGENT_SECRET using crypto.randomBytes
   - Ensure DATABASE_URL, POSTGRES_PASSWORD, DB_PASSWORD triple is consistent (derive password from DATABASE_URL, set POSTGRES_PASSWORD and DB_PASSWORD to match)
   - Print summary of what was set (mask secrets, show only first 4 chars)
   - Exit 0

2. Create `scripts/env/check.ts`:
   - Load `.env.local` (use scripts/lib/load-env.ts)
   - Check DATABASE_URL exists — fail if missing
   - Parse DATABASE_URL password, compare with POSTGRES_PASSWORD and DB_PASSWORD — fail if inconsistent
   - Check AUTH_SESSION_SECRET, SYNC_PACKAGE_SECRET, ADMIN_TOKEN are not placeholder values (not "replace-with-*", not empty, at least 24 chars)
   - `--production` mode: also check SITE_AGENT_SECRET, no placeholder secrets, warn if AUTH_MODE != strict
   - Print check results with pass/fail per item
   - Exit 1 if any fail, 0 if all pass

3. Modify `lib/db/postgres.ts` line 21:
   - Replace `process.env.DATABASE_URL || 'postgresql://localhost:5432/optical_disc_central'` with just `process.env.DATABASE_URL!`
   - Add comment: "DATABASE_URL 必须由环境变量提供, 缺失直接报错"
   - If DATABASE_URL is undefined, the Pool constructor will throw naturally — no silent fallback

4. Add to package.json scripts:
   - `"env:init": "tsx scripts/env/init.ts"`
   - `"env:check": "tsx scripts/env/check.ts"`
   - `"env:check:production": "tsx scripts/env/check.ts --production"`

5. Commit: `feat(r92): add env:init/env:check scripts, postgres.ts fail-closed`

---

### Task 2: Route cleanup — /volumes → /racks?view=volumes

**Files:**
- Modify: `components/dashboard/stats-cards.tsx:157-159` (href="/volumes" → href="/racks?view=volumes", update comment)
- Modify: `components/dashboard/command-center-panel.tsx:288` (href="/volumes" → href="/racks?view=volumes")
- Modify: `components/layout/app-shell.tsx` (guide steps referencing /volumes → /racks?view=volumes, data-testid references)
- Search all .tsx/.ts files for remaining href="/volumes" or Link to="/volumes" that are user-facing and update them

**Interfaces:**
- Consumes: /volumes redirect still exists (app/volumes/page.tsx)
- Produces: All user-facing links now go to /racks?view=volumes directly (bypassing redirect)

**Steps:**

1. In `components/dashboard/stats-cards.tsx`, change:
   - Comment: `存储使用率 (可点击 → /racks?view=volumes)`
   - `href="/volumes"` → `href="/racks?view=volumes"`

2. In `components/dashboard/command-center-panel.tsx`, change:
   - `href="/volumes"` → `href="/racks?view=volumes"`

3. In `components/layout/app-shell.tsx`, update guide steps:
   - Any reference to `/volumes` as a destination → `/racks?view=volumes`
   - Any `data-testid` referencing "volumes-page" → "volumes-view" (since it's now a sub-view, not a page)

4. Search entire app/ + components/ for any remaining user-visible `/volumes` link (href, Link to, router.push) and update to `/racks?view=volumes`. Exclude: the redirect file itself (app/volumes/page.tsx), test files, API routes.

5. Commit: `fix(r92): all /volumes user links → /racks?view=volumes`

---

### Task 3: Agent → 站点代理 user-visible text cleanup

**Files:**
- Modify: `components/ui/global-control-ball.tsx:766` (在线 Agent → 在线代理)
- Modify: `app/tasks/page.tsx` (7 occurrences: 等待站点 Agent → 等待站点代理)
- Modify: `app/settings/page.tsx:574` (站点端 Agent → 站点代理)
- Modify: `app/sync/page.tsx` (3 occurrences)
- Modify: `app/racks/page.tsx:1407` (查看站点 Agent → 查看站点代理)
- Modify: `components/layout/app-shell.tsx` (4 guide occurrences)
- Modify: `components/dashboard/welcome-banner.tsx` (Agent 控制 → 代理控制)
- Modify: `components/tasks/control-command-panel.tsx:29` (待 Agent → 待站点代理)
- Modify: `app/racks/page.tsx` (mock as data source type → use "本地调试" or just "empty")

**Interfaces:**
- Produces: All user-visible "Agent" replaced with "站点代理" or "代理"

**Steps:**

1. Global find-and-replace in user-visible strings only (not code comments, not variable names, not type definitions):
   - "站点 Agent" → "站点代理"
   - "Agent" (standalone, in user text) → "代理" or "站点代理" (context-dependent)
   - "Agent 控制" → "代理控制"
   - "待 Agent 拉取" → "待站点代理拉取"
   - "在线 Agent" → "在线代理"

2. In `app/racks/page.tsx`, the "mock" data source type:
   - Replace `isApiMode ? "empty" : "mock"` with just `"empty"` (API mode is always on, no mock fallback)
   - Remove the `type RacksDataSource = ApiRacksDataSource | "mock"` — just use `ApiRacksDataSource | "empty"`
   - Remove `setRacksDataSource("mock")` fallback — use `"empty"` instead

3. In `app/sites/page.tsx:549`, the raw field names display:
   - Replace `站点 {t.sourceCount ?? t.source_count} / 总控 {t.unifiedCount ?? t.unified_count}` with proper fallback that never shows raw field names
   - Use: `站点 {t.sourceCount ?? 0} / 总控 {t.unifiedCount ?? 0} / 差异 {t.countDiff ?? 0}`

4. Verify no remaining user-visible "Agent" (except in code-only contexts like variable names, type names, API route paths, env var names).

5. Commit: `fix(r92): Agent → 站点代理, mock → empty, field name cleanup`

---

### Task 4: Test cleanup — rewrite test-volumes, delete old self-checks, fix e2e references

**Files:**
- Rewrite: `scripts/e2e/test-volumes.ts` (test /racks?view=volumes instead of standalone /volumes)
- Delete: `components/check/__tests__/self-check.ts` (tests old 17-tab /check page)
- Check: `app/api/check/__tests__/self-check.ts` (may need delete or update)
- Modify: `scripts/e2e/test-console-usability-lift.ts:214-215` (href="/volumes" assertion → href="/racks?view=volumes")
- Modify: `scripts/e2e/test-card-layout.ts` (test /volumes as standalone → test redirect)
- Modify: `scripts/e2e/test-compatibility.ts:35` (test /volumes route → test redirect)
- Modify: `scripts/e2e/test-header-ux-lift.ts` (fetch /volumes → test redirect)
- Modify: `scripts/e2e/test-route-page-integration.ts:64` (map /volumes to page → map as redirect)
- Modify: `scripts/e2e/test-dark-mode.ts` (volumes page → redirect)

**Interfaces:**
- Consumes: /volumes and /check redirects still exist
- Produces: All e2e tests validate merged views or redirects, not old standalone pages

**Steps:**

1. Rewrite `scripts/e2e/test-volumes.ts`:
   - Test `/racks?view=volumes` page (200 OK)
   - Read source of `components/racks/volumes-view.tsx` for forbidden string checks
   - Test /volumes redirect (fetch with redirect:manual, verify 3xx to /racks?view=volumes)
   - Keep volume API tests (/api/volumes) as-is (API still exists)
   - Keep volume data source checks

2. Delete `components/check/__tests__/self-check.ts` entirely — it tests a page that no longer exists (17-tab /check)

3. Check and handle `app/api/check/__tests__/self-check.ts` — if it tests API routes that still exist, keep it; if it tests old page structure, delete or update

4. For each e2e test that references `/volumes` as a standalone page:
   - Change assertions to expect redirect or merged view
   - Update source file references from `app/volumes/page.tsx` to `components/racks/volumes-view.tsx`
   - Update href assertions from `/volumes` to `/racks?view=volumes`

5. Commit: `fix(r92): rewrite test-volumes, delete old self-checks, update e2e references`

---

### Task 5: Delete temp files + archive stale docs + package.json cleanup

**Files:**
- Delete: `test-login.js` (root temp file)
- Archive: `docs/summary/DEPLOYMENT_GUIDE.md` → move to `docs/archive/DEPLOYMENT_GUIDE-sprint3.md` with short redirect stub
- Check for other stale docs in `docs/summary/` or `docs/superpowers/` that should be archived
- Modify: `package.json` — remove outdated sprint scripts (test:r83.*, sprint-2*, and other clearly outdated entries)

**Steps:**

1. Delete `test-login.js` from root directory

2. Move `docs/summary/DEPLOYMENT_GUIDE.md` to `docs/archive/DEPLOYMENT_GUIDE-sprint3.md`
   - Create `docs/archive/` directory if needed
   - Replace original with a short stub pointing to `docs/operations/deployment.md`

3. Scan `docs/summary/` and `docs/superpowers/specs/` and `docs/superpowers/plans/` for files that are clearly stale (pre-R.83, reference old architecture). List them but don't archive unless obviously outdated.

4. Clean up package.json scripts:
   - Remove all `test:r83.*-whitelist`, `test:r83.*-api`, `test:r83.*-ui` scripts (they point to deleted/invalid self-checks)
   - Remove `test:r83.3-e2e`, `test:r83.4-e2e` (old e2e paths)
   - Remove sprint-2* scripts that are clearly development-era scratch (keep any that are still useful)
   - Keep all e2e:*, audit:*, import:*, db:*, smoke:*, baseline:* scripts
   - Add `env:init`, `env:check`, `env:check:production` (already added in Task 1)

5. Commit: `chore(r92): delete temp files, archive stale docs, clean package.json`

---

### Task 6: Documentation updates

**Files:**
- Rewrite: `docs/operations/deployment.md` (add env init/check, volume password handling, release gate section)
- Update: `README.md` (add env:init, update page list, honest capability boundaries)
- Update: `docs/summary/PROJECT_STATUS.md` (reflect R.92 closeout)
- Update: `docs/summary/ROADMAP.md` (mark R.92 items)

**Steps:**

1. Update `docs/operations/deployment.md`:
   - Add section 3.2: "环境初始化与检查" (pnpm env:init, pnpm env:check, volume密码不一致处理)
   - Update "本地首次启动" section to include `pnpm env:init` before `pnpm db:up`
   - Add release gate section: list all commands that must pass before merge
   - Add troubleshooting: SCRAM authentication → run env:check, then db:down:volumes + db:up + db:init

2. Update `README.md`:
   - Add `pnpm env:init` to quick start
   - Add `pnpm env:check` to pre-commit checks
   - Honest capability boundaries section (what's complete, what's blocked, what's out of scope)
   - Update R.92 status note

3. Update `docs/summary/PROJECT_STATUS.md`:
   - Add R.92 closeout section
   - Honest status per requirement area

4. Update `docs/summary/ROADMAP.md`:
   - Mark R.92 closeout items as done

5. Commit: `docs(r92): deployment env preflight, README, PROJECT_STATUS updates`

---

### Task 7: Final gates

**Steps:**

1. Run `pnpm env:check` — must pass
2. Run `pnpm exec tsc --noEmit` — must pass
3. Run `pnpm build` — must pass
4. Run `pnpm audit:page-scope` — must pass
5. Run `pnpm audit:product-copy` — must pass
6. Run `pnpm audit:data-coverage` — must pass
7. Run `pnpm audit:api-mode-no-fallback` — must pass
8. Run `pnpm audit:page-no-todo` — must pass
9. Push to origin
10. Verify PR #7 is updated
