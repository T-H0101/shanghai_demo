# 统一光盘库管理平台 - 虚拟接口设计

> ⚠️ 本文档描述的接口为**虚拟接口**，用于指导后续真实后端接入，不代表当前 Demo 已实现。

---

## 1. 接口总览

| 接口 | 方法 | 路径 | 对应页面 | Mock 实现 |
|------|------|------|----------|-----------|
| 获取站点列表 | GET | `/api/sites` | `/sites` | `lib/mock/sites.ts` |
| 获取站点详情 | GET | `/api/sites/:id` | `/sites` 详情 | `lib/mock/sites.ts` |
| 新建站点 | POST | `/api/sites` | `/sites` 新建 Dialog | 无 |
| 更新站点 | PUT | `/api/sites/:id` | `/sites` 编辑 | 无 |
| 同步站点 | POST | `/api/sites/sync` | `/sites` 同步按钮 | 无 |
| 获取文件检索结果 | GET | `/api/files/search` | `/search` | `lib/mock/search.ts` |
| 获取任务列表 | GET | `/api/tasks` | `/tasks` | `lib/mock/tasks.ts` |
| 新建任务 | POST | `/api/tasks` | `/tasks` 新建 Dialog | 无 |
| 更新任务状态 | PUT | `/api/tasks/:id` | `/tasks` 操作按钮 | 无 |
| 获取盘架列表 | GET | `/api/racks` | `/racks` | `lib/mock/racks.ts` |
| 盘笼移位登记 | POST | `/api/racks/transfer` | `/racks` 移位 | 无 |
| 获取用户列表 | GET | `/api/users` | `/users` | `lib/mock/users.ts` |
| 更新用户权限 | PUT | `/api/users/:id/permissions` | `/users` 权限树 | 无 |
| 获取审计日志 | GET | `/api/logs` | `/logs` | `lib/mock/audit.ts` |
| 导出日志 | GET | `/api/logs/export` | `/logs` 导出 | 无 |
| 获取系统设置 | GET | `/api/settings` | `/settings` | `lib/mock/settings.ts` |
| 更新系统设置 | PUT | `/api/settings` | `/settings` 保存 | 无 |

---

## 2. 站点管理接口

### 2.1 GET /api/sites

**用途**：获取站点列表

**请求参数**：
| 参数 | 类型 | 必填 | 说明 |
|------|------|:----:|------|
| keyword | string | ❌ | 搜索关键词（名称/编码/机房） |
| status | string | ❌ | 状态筛选（online/degraded/offline） |
| page | number | ❌ | 页码，默认 1 |
| pageSize | number | ❌ | 每页数量，默认 20 |

**响应字段**：
```json
{
  "data": [
    {
      "id": "s1",
      "name": "上海研发中心",
      "code": "SH-RD-01",
      "status": "online",
      "ip": "10.12.8.101",
      "port": 8443,
      "datacenter": "浦东 IDC-A",
      "contact": "李明",
      "contactPhone": "138****2201",
      "deviceCount": 48,
      "lastSyncAt": "2026-05-18 14:28:03",
      "syncStatus": "synced",
      "storageUsedPercent": 68,
      "storageTotal": "120 TB",
      "storageUsed": "81.6 TB",
      "region": "华东",
      "ssoEnabled": true,
      "rackCount": 6,
      "taskCount": 12,
      "description": "核心研发数据归档站点，承载临床试验与影像数据。"
    }
  ],
  "total": 6,
  "page": 1,
  "pageSize": 20
}
```

**当前 Demo 实现**：
- 文件：`lib/mock/sites.ts` → `sites` 数组
- 筛选逻辑：在页面组件 `useState` 中通过 `filter()` 实现

---

### 2.2 POST /api/sites

**用途**：新建站点

**请求参数**：
```json
{
  "name": "深圳数据中心",
  "code": "SZ-DC-07",
  "datacenter": "南山 IDC-G",
  "ip": "10.30.1.100",
  "port": 8443,
  "contact": "张三",
  "contactPhone": "139****1234"
}
```

**响应字段**：
```json
{
  "id": "s7",
  "name": "深圳数据中心",
  "code": "SZ-DC-07",
  "status": "online",
  "syncStatus": "pending",
  "createdAt": "2026-05-18 20:00:00"
}
```

**当前 Demo 实现**：仅 Dialog 表单 → `useState` 添加 → Toast 提示，无 API 调用

---

### 2.3 POST /api/sites/sync

**用途**：触发全量同步

**请求参数**：无

**响应字段**：
```json
{
  "success": true,
  "message": "全量同步任务已提交",
  "syncedAt": "2026-05-18 20:01:00"
}
```

**当前 Demo 实现**：`/sites` 页面 `handleSync()` → `setTimeout` 1.5s → Toast 提示

---

## 3. 任务管理接口

### 3.1 GET /api/tasks

**用途**：获取任务列表

**请求参数**：
| 参数 | 类型 | 必填 | 说明 |
|------|------|:----:|------|
| type | string | ❌ | 任务类型（backup/restore/inspect/burn） |
| status | string | ❌ | 任务状态（running/paused/failed/completed/queued） |
| keyword | string | ❌ | 搜索关键词 |
| page | number | ❌ | 页码 |
| pageSize | number | ❌ | 每页数量 |

**响应字段**：
```json
{
  "data": [
    {
      "id": "t1",
      "name": "临床试验数据回迁_A09",
      "type": "restore",
      "status": "running",
      "priority": "high",
      "progress": 78,
      "siteName": "上海研发中心",
      "siteCode": "SH-RD-01",
      "operator": "张建国",
      "startedAt": "2026-05-18 13:00:00",
      "updatedAt": "2026-05-18 14:28:00",
      "speed": "420 MB/s",
      "discNo": "DISC-2026-00891",
      "estimatedEnd": "14:45"
    }
  ],
  "total": 7
}
```

**当前 Demo 实现**：直接使用 `lib/mock/tasks.ts` → `tasks` 数组

---

### 3.2 POST /api/tasks

**用途**：新建任务

**请求参数**：
```json
{
  "name": "临床试验数据回迁_A10",
  "type": "restore",
  "priority": "high",
  "siteCode": "SH-RD-01",
  "discNo": "DISC-2026-00900"
}
```

**响应字段**：
```json
{
  "id": "t8",
  "name": "临床试验数据回迁_A10",
  "type": "restore",
  "status": "queued",
  "priority": "high",
  "createdAt": "2026-05-18 20:05:00"
}
```

**当前 Demo 实现**：Dialog → `useState` 添加 → Toast 提示

---

### 3.3 PUT /api/tasks/:id

**用途**：更新任务状态（暂停/恢复/重试）

**请求参数**：
```json
{
  "action": "pause" | "resume" | "retry" | "reset",
  "priority": "high" | "normal"  // 仅优先执行时传递
}
```

**响应字段**：
```json
{
  "id": "t1",
  "status": "paused",
  "updatedAt": "2026-05-18 20:06:00"
}
```

**当前 Demo 实现**：`/tasks` 页面各操作 handler → `useState` 更新 → Toast 提示

---

## 4. 统一检索接口

### 4.1 GET /api/files/search

**用途**：全局文件检索

**请求参数**：
| 参数 | 类型 | 必填 | 说明 |
|------|------|:----:|------|
| keyword | string | ❌ | 关键词 |
| site | string | ❌ | 站点筛选 |
| department | string | ❌ | 部门筛选 |
| discNo | string | ❌ | 光盘编号 |
| volume | string | ❌ | 存储卷 |
| fileType | string | ❌ | 文件类型 |
| page | number | ❌ | 页码 |
| pageSize | number | ❌ | 每页数量，默认 20 |

**响应字段**：
```json
{
  "data": [
    {
      "id": "f1",
      "fileName": "临床试验报告_A09_终稿.pdf",
      "path": "/archive/2026/clinical/A09/final",
      "size": "128.5 MB",
      "sizeBytes": 134742016,
      "siteName": "上海研发中心",
      "siteCode": "SH-RD-01",
      "discNo": "DISC-2026-00891",
      "rackSlot": "R-A12 / 槽位 08",
      "department": "研发中心",
      "fileType": "PDF",
      "createdAt": "2026-05-15 09:22:11",
      "volume": "VOL-CLINICAL-01"
    }
  ],
  "total": 8
}
```

**当前 Demo 实现**：使用 `lib/mock/search.ts` → `searchFiles` 数组 + `filter()` 联动

---

### 4.2 POST /api/files/search/restore

**用途**：从检索结果发起回迁任务

**请求参数**：
```json
{
  "fileId": "f1",
  "discNo": "DISC-2026-00891"
}
```

**响应字段**：
```json
{
  "taskId": "t9",
  "message": "回迁任务已创建，等待调度执行"
}
```

**当前 Demo 实现**：点击"发起回迁" → Toast 提示，无 API 调用

---

## 5. 审计日志接口

### 5.1 GET /api/logs

**用途**：获取审计日志列表

**请求参数**：
| 参数 | 类型 | 必填 | 说明 |
|------|------|:----:|------|
| tab | string | ❌ | 日志类型（operations/security/system/task/compliance） |
| site | string | ❌ | 站点筛选 |
| operator | string | ❌ | 操作人筛选 |
| result | string | ❌ | 结果筛选（success/failure/warning） |
| page | number | ❌ | 页码 |
| pageSize | number | ❌ | 每页数量 |

**响应字段**：
```json
{
  "data": [
    {
      "id": "al1",
      "logId": "LOG-20260518-12847",
      "type": "operation",
      "typeLabel": "操作流水",
      "taskType": "RESTORE",
      "siteName": "上海研发中心",
      "operator": "张建国",
      "operatedAt": "2026-05-18 14:28:03",
      "result": "success",
      "summary": "发起临床试验数据回迁任务 A09",
      "signatureValid": true
    }
  ],
  "total": 6
}
```

**当前 Demo 实现**：使用 `lib/mock/audit.ts` → `auditLogs` 数组

---

### 5.2 GET /api/logs/export

**用途**：导出审计日志

**请求参数**：
| 参数 | 类型 | 必填 | 说明 |
|------|------|:----:|------|
| format | string | ✅ | 导出格式（excel/csv/json） |
| tab | string | ❌ | 日志类型 |
| site | string | ❌ | 站点筛选 |
| startDate | string | ❌ | 开始日期 |
| endDate | string | ❌ | 结束日期 |

**响应**：文件下载流

**当前 Demo 实现**：点击导出按钮 → Toast 提示，无文件生成

---

## 6. 系统设置接口

### 6.1 GET /api/settings

**用途**：获取系统设置

**响应字段**：
```json
{
  "sync": {
    "realtimeSync": true,
    "scheduledIntervalMinutes": 15,
    "fullSyncTime": "02:00",
    "retryCount": 3,
    "retryIntervalSeconds": 60,
    "consistencyCheckTime": "04:00"
  },
  "alert": {
    "siteOfflineThresholdMinutes": 5,
    "hardwareAnomalyThreshold": 3,
    "taskTimeoutMinutes": 120,
    "capacityWarningPercent": 85,
    "emailNotification": true,
    "emailRecipients": "ops@corp.example.com"
  },
  "security": {
    "jwtExpiryHours": 8,
    "loginFailLockThreshold": 5,
    "ipLockPolicy": "moderate",
    "sensitiveDataEncryption": true
  },
  "task": {
    "restorePriority": true,
    "backupConcurrency": 8,
    "inspectSamplePercent": 10,
    "logRetentionDays": 365
  },
  "services": [
    {
      "id": "svc1",
      "name": "API Gateway",
      "status": "healthy",
      "cpu": 24,
      "memory": 42,
      "disk": 35,
      "apiLatencyMs": 12,
      "uptime": "99.98%",
      "version": "v2.4.1"
    }
  ]
}
```

**当前 Demo 实现**：使用 `lib/mock/settings.ts` → `defaultSettings`

---

### 6.2 PUT /api/settings

**用途**：更新系统设置

**请求参数**：同 `GET /api/settings` 响应结构（可部分更新）

**响应字段**：
```json
{
  "success": true,
  "message": "设置已保存，将在下一同步周期生效"
}
```

**当前 Demo 实现**：点击保存 → Toast 提示，`useState` 更新，无 API 调用

---

## 7. 用户与权限接口

### 7.1 GET /api/users

**用途**：获取用户列表

**响应字段**：
```json
{
  "data": [
    {
      "id": "u1",
      "username": "zhangjg",
      "displayName": "张建国",
      "department": "信息技术部",
      "role": "super_admin",
      "roleLabel": "超级管理员",
      "accessibleSites": ["全部站点"],
      "status": "active",
      "lastLoginAt": "2026-05-18 14:00:22",
      "permissionSyncStatus": "synced",
      "email": "zhangjg@corp.example.com"
    }
  ],
  "total": 5
}
```

**当前 Demo 实现**：使用 `lib/mock/users.ts` → `users` 数组

---

### 7.2 PUT /api/users/:id/permissions

**用途**：更新用户权限

**请求参数**：
```json
{
  "sites": [true, true, false, false, true, true],
  "allSiteNotify": true,
  "devices": [...],
  "volumes": [...],
  "tasks": [...],
  "logs": [...]
}
```

**响应字段**：
```json
{
  "success": true,
  "permissionSyncStatus": "syncing"
}
```

**当前 Demo 实现**：权限树 Switch 仅 UI 修改，无 API 调用

---

## 8. 盘架管理接口

### 8.1 GET /api/racks

**用途**：获取盘架列表

**响应字段**：
```json
{
  "data": [
    {
      "id": "r1",
      "rackId": "R-A12",
      "siteName": "上海研发中心",
      "siteCode": "SH-RD-01",
      "datacenter": "浦东 IDC-A",
      "cages": ["笼-A", "笼-B"],
      "totalSlots": 24,
      "usedSlots": 18,
      "usagePercent": 75,
      "status": "normal",
      "lastSyncAt": "2026-05-18 14:28:03",
      "slots": [
        { "id": "slot-1", "index": 1, "occupied": true, "discNo": "DISC-2026-00800", "label": "槽1" },
        { "id": "slot-2", "index": 2, "occupied": false }
      ]
    }
  ],
  "total": 6
}
```

**当前 Demo 实现**：使用 `lib/mock/racks.ts` → `racks` 数组

---

### 8.2 POST /api/racks/transfer

**用途**：盘笼移位登记

**请求参数**：
```json
{
  "rackId": "R-A12",
  "cageId": "笼-A",
  "fromSite": "上海研发中心",
  "toSite": "北京总部机房",
  "operator": "张建国",
  "reason": "数据迁移"
}
```

**响应字段**：
```json
{
  "transferId": "TRF-20260518-001",
  "status": "pending",
  "createdAt": "2026-05-18 20:10:00"
}
```

**当前 Demo 实现**：移位按钮仅有 UI，无 Dialog 和 API 调用

---

## 9. 接口实现状态汇总

| 接口 | 状态 | 说明 |
|------|------|------|
| GET /api/sites | ✅ 已 Mock | 直接使用 mock 数据 |
| POST /api/sites | ❌ 未实现 | 仅 Dialog，无 API |
| POST /api/sites/sync | ❌ 未实现 | 仅 Toast，无 API |
| GET /api/tasks | ✅ 已 Mock | 直接使用 mock 数据 |
| POST /api/tasks | ❌ 未实现 | 仅 Dialog，无 API |
| PUT /api/tasks/:id | ❌ 未实现 | 仅 useState，无 API |
| GET /api/files/search | ✅ 已 Mock | filter 联动 |
| POST /api/files/search/restore | ❌ 未实现 | 仅 Toast，无 API |
| GET /api/logs | ✅ 已 Mock | filter 联动 |
| GET /api/logs/export | ❌ 未实现 | 仅 Toast，无文件 |
| GET /api/settings | ✅ 已 Mock | 直接使用 mock 数据 |
| PUT /api/settings | ❌ 未实现 | 仅 Toast，无 API |
| GET /api/users | ✅ 已 Mock | 直接使用 mock 数据 |
| PUT /api/users/:id/permissions | ❌ 未实现 | 仅 UI，无 API |
| GET /api/racks | ✅ 已 Mock | 直接使用 mock 数据 |
| POST /api/racks/transfer | ❌ 未实现 | 无 UI，无 API |

---

*文档生成时间：2026-05-18*